// zadefinovat premenne: string, number, boolean 


let veta = ('Hello World')
let number = (34, 3.14)
let userWoman = true

console.log(veta, number, userWoman)

console.log(typeof veta) // vypíše, o co jaký typ proměnné se jedná

let name = 'Katerina'  // přiřazovací operátor

let age = 30 
console.log(age + 5) // aritmetické operace - násobení, moduly, sčítání, odečítání, dělení, exponentation

let vyska = 190
let vek = 30
console.log(vyska + vek)


 const levaStrana = 50
 const pravaStrana = 20
 console.log(levaStrana === pravaStrana) // false, třetí rovná se (zahrnuje typ i hodnotu promenne) porovnavaci operatory - rovná se, striktní rovná se, nerovná se, strikční nerovná se, větší než, menší, větší nebo rovno, menší nebo rovno, znamenka, pro podmínky


 // logické and (&&) / or || , negace ! podmínkované testy moc ne

 let x = 10 // přiřazovací operatory, součtové přiřazení (+=) a rzodílové přiřazeí (-=)
 let y = 5 
 let fff = x + y
 x += 10 
 x = x + 10  // zjednodušeně je to řádek výše


 // inkrementační ++ , --  ... navýšíme hodnotu o jednu,  namísto x = x + 1 ale x++ , x-- ... používá se při cyklusu (ukončivací podmínku)



// do console.log vypiste: 2 premenne a jejich součet, odečet, násobení, dělení
// dale: porovnat dvě proměnné i jejich typy
// dale: inkrementujeme a dekrementujeme premenou c. 1 o hodnotu preměnné č. 2
// dale: inkremetujeme hodnotu premenné č. 1 o +1

let sirka = 5
let hloubka = 10
console.log(sirka + hloubka)
console.log(sirka * hloubka)
console.log(hloubka % sirka)
console.log(sirka - hloubka)

const minute = 60
const hour = 58
console.log(minute === hour)

let a = 10;
let b = 3;
console.log(a+=b)
console.log(a-=b)
console.log(a++)

console.error()


